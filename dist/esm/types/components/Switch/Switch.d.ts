import { SwitchProps } from '../../types/switch';
import './index.css';
declare const Switch: {
    ({ prefixCls, checked, onChange, onClick, disabled, className, style, defaultChecked, value }: SwitchProps): import("react").JSX.Element;
    displayName: string;
};
export default Switch;
