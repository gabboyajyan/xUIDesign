name: xUIDesign

on: push

jobs:
  super-lint:
    name: X UI Design
    runs-on: ubuntu-latest
    steps:
      - name: Run xUIDesign
        uses: gabboyajyan/xUIDesign@main
        env:
          DEFAULT_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
